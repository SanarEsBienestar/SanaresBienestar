---
interface Props {
    Name: string;
    Details: string;
    StarNum: Number;
    Date: string    
}

const { Name, Details, StarNum, Date } = Astro.props;

const starCount = Math.max(1, Math.min(StarNum, 5));
---

<blockquote class="flex flex-col justify-around rounded-lg bg-white p-6 shadow-sm sm:p-8">
    <div class="flex items-center gap-4">
      <div class="flex flex-col items-center justify-center w-full">
        
        <div class="flex justify-center gap-0.5 text-yellowburn">
            {Array.from({ length: StarNum }, (_, index) => (
            <svg
              key={index} // Usamos el índice como key
              xmlns="http://www.w3.org/2000/svg"
              class="h-5 w-5"
              viewBox="0 0 20 20"
              fill="currentColor"
            >
              <path
                d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"
              />
            </svg>
          ))}
        </div>
        <p class="text-lg font-medium text-gray">{ Name }</p>
      </div>
    </div>
    <p class="mt-4 text-gray text-center text-balanced">{ Details }</p> 
    <p class="mt-4 text-xs text-grayblue text-center">{ Date }</p>
  </blockquote>